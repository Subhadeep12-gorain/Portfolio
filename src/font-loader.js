// Font CSS loaded asynchronously so it does NOT block initial render.
// Vite bundles these into a separate CSS chunk that loads after first paint.
import '@fontsource/shippori-mincho';
import '@fontsource/zen-kaku-gothic-new';
import '@fontsource/jetbrains-mono';
